// Magnetar ImGui config — included via IMGUI_USER_CONFIG define.
// Ensures glad.h is visible to ImGui's OpenGL3 backend when
// IMGUI_IMPL_OPENGL_LOADER_CUSTOM is defined.
#pragma once
#include <glad.h>
